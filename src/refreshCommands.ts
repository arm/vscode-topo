import { PACKAGE_NAME } from './manifest';

export const refreshTargetStateCommand = `${PACKAGE_NAME}.refreshTargetState`;
export const refreshTargetContainersCommand = `${PACKAGE_NAME}.refreshTargetContainers`;
